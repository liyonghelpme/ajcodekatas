
- Compiler
	- Implement CompileTopCommand(PepsiMachine machine)
		accepting
		
		Object()
		NewClass : Object()
		NewClass : Object(variable1 variable2)
		aGlobal := [...]
		[....]
		NewClass methodnamepars [....]

- Review Smell code
	- big switch on ByteCodes
	- use of Machine in ExecutionBlock
	
- Compiler to C#

- Exceptions classes

- Remove initial prototype Object from PepsiMachine

- Evaluator support of "Object()" and "NewProto : Proto(a b)"

